name: CI/CD Pipeline

on:
  push:
    branches: [ main ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    
    steps:
    # Step 1: Show we're running
    - name: Check starting
      run: echo "🚀 Starting CI/CD Pipeline..."
    
    # Step 2: Get code
    - name: Checkout code
      uses: actions/checkout@v3
    
    # Step 3: SHOW files (for debugging)
    - name: List all files
      run: |
        echo "📂 Current directory:"
        pwd
        echo ""
        echo "📁 All files and folders:"
        ls -la
        echo ""
        echo "📄 Dockerfile exists?"
        if [ -f "Dockerfile" ]; then
          echo "✅ YES! Dockerfile found!"
          echo "Content:"
          cat Dockerfile
        else
          echo "❌ NO Dockerfile found!"
          exit 1
        fi
    
    # Step 4: Login to Docker Hub
    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
    
    # Step 5: Build with EXPLICIT Dockerfile path
    - name: Build and push Docker image
      uses: docker/build-push-action@v4
      with:
        context: .
        file: ./Dockerfile
        push: true
        tags: |
          ${{ secrets.DOCKER_USERNAME }}/my-app:latest
          ${{ secrets.DOCKER_USERNAME }}/my-app:${{ github.sha }}
    
    # Step 6: Success!
    - name: Celebrate
      run: |
        echo "🎉🎉🎉 PIPELINE SUCCESSFUL! 🎉🎉🎉"
        echo "✅ Image built and pushed to Docker Hub!"


- name: Run tests
  run: |
    echo "Installing test dependencies..."
    pip install pytest
    echo "Running tests..."
    # Add your test command here


- name: Send Slack notification
  if: success()
  uses: 8398a7/action-slack@v3
  with:
    status: ${{ job.status }}
    channel: '#devops'


- name: Scan with Trivy
  uses: aquasecurity/trivy-action@master
  with:
    image-ref: ${{ secrets.DOCKER_USERNAME }}/my-app:latest
    format: 'sarif'
    output: 'trivy-results.sarif'


